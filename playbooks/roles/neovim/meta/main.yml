---
galaxy_info:
  role_name: neovim
  author: "{{ lookup('env', 'USER') }}"
  description: Installs and configures Neovim with custom plugins and keymaps
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions: all
    - name: Ubuntu
      versions: all
    - name: EL
      versions: all
    - name: opensuse
      versions: all
dependencies: []
