---
galaxy_info:
  role_name: base_packages
  author: "{{ lookup('env', 'USER') }}"
  description: Installs base packages for a workstation across Debian, RedHat, and SUSE families
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions: all
    - name: Ubuntu
      versions: all
    - name: EL
      versions: all
    - name: opensuse
      versions: all
dependencies: []
