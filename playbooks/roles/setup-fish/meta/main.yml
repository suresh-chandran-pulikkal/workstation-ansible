---
galaxy_info:
  role_name: setup_fish
  author: "{{ lookup('env', 'USER') }}"
  description: Installs fish shell and starship prompt, deploys custom config from git
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions: all
    - name: Ubuntu
      versions: all
    - name: EL
      versions: all
    - name: opensuse
      versions: all
dependencies: []
